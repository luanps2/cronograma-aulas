# Server Configuration
PORT=5000
NODE_ENV=development

# Database Connection (PostgreSQL via Supabase)
# CRITICAL: Use Session Pooler connection string (port 6543) for production
# Session Pooler is required for IPv4 connections from Render and other cloud providers
#
# Format: postgresql://postgres:[PASSWORD]@[HOST]:6543/postgres
# Note: Port 6543 (Session Pooler) instead of 5432 (Direct Connection)
#
# For local development:
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/senactech

# For production (Supabase Session Pooler - IPv4):
# DATABASE_URL=postgresql://postgres:[YOUR_DB_PASSWORD]@db.xyz.supabase.co:6543/postgres
#
# Find your credentials in Supabase Dashboard:
# Settings > Database > Connection String > Session Pooler (Transaction mode)

# Security
# Secret key for signing JWT tokens. Change this in production!
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_SECRET

# Google OAuth
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID_HERE
GOOGLE_API_KEY=YOUR_GOOGLE_API_KEY_HERE

# Frontend Connection
# URL where the frontend is hosted (for CORS and OAuth redirects)
FRONTEND_URL=http://localhost:5173
